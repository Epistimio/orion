experiment:
    name: voila_voici
    pool_size: 1
    max_trials: 100
    max_broken: 5

    algorithms: 'random'

    strategy: NoParallelStrategy

database:
  type: 'mongodb'
  name: 'orion_test'
  host: 'mongodb://user:pass@localhost'
