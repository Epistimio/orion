name: voila_voici

pool_size: 1
max_trials: 100
max_broken: 5

algorithms:
  gradient_descent:
    learning_rate: 0.1
    # dx_tolerance: 1e-7

producer:
    strategy: NoParallelStrategy

database:
  type: 'mongodb'
  name: 'orion_test'
  host: 'mongodb://user:pass@localhost'
