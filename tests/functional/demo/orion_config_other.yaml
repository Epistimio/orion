experiment:
    name: voila_voici

    pool_size: 1
    max_trials: 20
    max_broken: 5

    algorithm:
      gradient_descent:
        learning_rate: 0.1
        # dx_tolerance: 1e-7

worker:
    user_script_config: configuration
