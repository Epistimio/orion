name: demo_random_search

pool_size: 2
max_trials: 400

algorithms: random

producer:
    strategy: NoParallelStrategy

database:
  type: 'mongodb'
  name: 'orion_test'
  host: 'mongodb://user:pass@localhost'
