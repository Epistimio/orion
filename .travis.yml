sudo: false
language: python

jobs:
  include:
      - env: TOXENV=none
        python: 3.6
        install: skip
        script: ./conda/conda_build.sh

install:
  - pip install tox
script:
  - tox

# deploy:
#   skip_cleanup: true
#   provider: pypi
#   user: "..."
#   password:
#     secure: "..."
#   on:
#     branch: master
#   distributions: "sdist bdist_wheel"
